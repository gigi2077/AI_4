[build]
  command = "pip install -r requirements.txt && python update_data.py && node build.js"
  publish = "dist"
  # REMOVE the edge_functions_import_map line.

[build.environment]
  PYTHON_VERSION = "3.9"

[[edge_functions]]
  path = "/api/prompt"
  function = "prompt"